server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # This replaces the Vite Proxy
    location /api/ {
        # proxy_pass http://backend:3000; # initially 'backend' was the service name in docker-compose
        proxy_pass http://backend-service:3000;  # Now everything is 'backend-service' and port 3000
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}